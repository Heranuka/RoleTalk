name: speak_sim
description: RoleTalk MVP — Ролевое общение
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  audioplayers: ^6.1.0
  path_provider: ^2.1.4
  record: ^6.2.0
  google_fonts: ^8.0.2
  shared_preferences: ^2.3.3
  crypto: ^3.0.6
  google_sign_in: ^7.2.0
  share_plus: ^12.0.2
  uuid: ^4.5.1
  flutter_local_notifications: ^21.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/